box: wercker/golang@1.3.2
build:
  steps:
    -  script:
        name: test script
        code: |-
          sudo apt-get update -qq
          sudo apt-get install -y golang
          sudo apt-get install -y cpio realpath squashfs-tools
          sudo apt-get install -y strace gdb libcap-ng-utils

          go version

          #sudo go get code.google.com/p/go.tools/cmd/cover
          #sudo go get code.google.com/p/go.tools/cmd/vet
          #sudo go get github.com/appc/spec/...

          ./test
          ./test-functional
